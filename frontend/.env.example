# Leave VITE_API_BASE_URL unset in production when the frontend and API share
# the same origin through a reverse proxy. The app will call /api/v1 by default.
# VITE_API_BASE_URL=https://example.com/api/v1

VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:3001
