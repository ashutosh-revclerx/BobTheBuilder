{
  "name": "bob-the-builder",
  "private": true,
  "scripts": {
    "dev": "node scripts/run-workspaces.mjs dev",
    "preview": "node scripts/run-workspaces.mjs preview",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
